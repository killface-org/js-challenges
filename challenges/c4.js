/*
Array modulus filter.

Create a function called modulusFilter that takes two parameters. The first parameter is an array of numbers, and the
second parameter is an integer. Return an array that contains the values from the input array, that are evenly divisible
by the second input parameter.

Examples:

Call: modulusFilter([1,2,3,4,5,6], 2)
Returns: [2,4,6]

Call: modulusFilter([25,8,3,5,15], 5)
Returns: [25,5,15]

*/

//Start writing your code.