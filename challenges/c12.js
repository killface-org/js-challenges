/*
Sum arrays in an array.

Create a function called sumArrays that takes one parameter. The input parameter will be array where each element can
either be an array of integers, or a single integer value. The goal of the function is to return an array where each
element is either the sum of the corresponding input arrays element, or if the input element is a single integer value
then return the same value.

Examples:

Call: sumArrays([[1,2,3],[4,4,4],13,[3,4,5]])
Returns: [6,12,13,12]

Call: sumArrays([1,2,3,[1,2,3],4,5,6,[4,5,6]])
Returns: [1,2,3,6,4,5,6,15]

Useful References:
https://developer.mozilla.org/de/docs/Web/JavaScript/Reference/Global_Objects/Array/isArray

*/

//Start writing your code.