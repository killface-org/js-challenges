/*
Array multiplier.

Create a function called multiplyArray that takes two parameters. The first parameter is an array of numbers, and the
second parameter is an integer to multiply each value in the array by.

Examples:

Call: multiplyArray([1,2,3], 2)
Returns: [2,4,6]

Call: multiplyArray([8,3,5], 4)
Returns: [32, 12, 20]

*/

//Start writing your code.